import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  images: {
    domains: [
      'media.licdn.com',
      'play-lh.googleusercontent.com',
      'www.neosohojakarta.com',
      'mms.img.susercontent.com',
      'www.eatsagala.id',
    ],
  },
  // ...other Next.js config options
}

export default nextConfig